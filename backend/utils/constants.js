// Constants
const INTERVIEW_STATUS = {
  PENDING: 'pending',
  IN_PROGRESS: 'in_progress',
  COMPLETED: 'completed'
};

const USER_ROLES = {
  USER: 'user',
  ADMIN: 'admin'
};

module.exports = { INTERVIEW_STATUS, USER_ROLES };